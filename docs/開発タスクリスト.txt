開発タスクリスト（優先度・ステータス反映版）

最優先タスク

カート機能:

[ ] カートの最大商品数オーバー時の振る舞い実装 (Controller/Exception HandlingでのHTTPステータスとJSONレスポンスの返却)

高優先度タスク

テスト環境の整備と自動テストの導入

[ ] REST Assuredの導入と自動APIテストの記述

中優先度タスク

カート機能:

[ ] カートの「最終更新日時」更新トリガーの実装（ユーザーログイン時を含む）

[ ] 注文・購入処理機能

中断（Suspended）タスク

主要機能の実装

ログイン・ユーザー管理機能:

[ ] パスワードリセット機能の実装

理由: 環境起因の課題解決に時間を要しており、開発の複雑化と全体の進捗に影響を与えているため、一旦中断します。この機能は将来的に必要になった際に改めて検討します。

カートエンティティの「最終更新日時」機能の実装

理由: 現状、原因不明の課題解決に時間を要しており、開発の複雑化と全体の進捗に影響を与えているため、一旦中断します。この機能は将来的に必要になった際に改めて検討します。

実施済み作業: Cart.javaからlastModifiedDateフィールドと関連アノテーションをコメントアウト。

完了したタスク

[x] java.lang.StackOverflowErrorの解消（2025-06-26完了）

[x] カートエンティティに「最終更新日時」フィールドを追加し、DBスキーマ更新（一度実装後、中断・コメントアウト）

残りの基本的なCRUD APIの実装とテスト

[x] PUT /api/products/{id} の実装とテスト

[x] DELETE /api/products/{id} の実装とテスト

エラーハンドリングの強化

[x] 無効な入力データに対するハンドリング

[x] リソースが見つからない場合（404 Not Found）の標準化

主要機能の実装

ログイン・ユーザー管理機能:

[x] ユーザー登録API (/api/users/register) のエラー調査と修正

[x] ユーザー認証（ログイン、ログアウト）の実装

[x] ログイン試行回数制限とアカウントロックのロジック実装

カート機能:

[x] CartServiceでのビジネスロジック実装（商品追加、数量変更、削除、カート内容の表示など）
